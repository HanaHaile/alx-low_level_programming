#include <stdio.h>
#include "main.h"

/**
 * man - prints the name of the program
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: Always 0 9success)
 */
int main(int argc _arttribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
